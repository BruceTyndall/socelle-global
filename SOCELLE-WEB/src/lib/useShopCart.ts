// useShopCart — re-export wrapper for shop/useShopCart
// Separate from useCart.ts (portal commerce — NEVER MODIFY).
export { useShopCart } from './shop/useShopCart';
export type { CartItem, CartSummary } from './shop/types';
